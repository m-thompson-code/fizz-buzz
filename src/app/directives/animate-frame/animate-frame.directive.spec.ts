import { AnimateFrameDirective } from './animate-frame.directive';

describe('AnimateFrameDirective', () => {
    it('should create an instance', () => {
        const directive = new AnimateFrameDirective();
        expect(directive).toBeTruthy();
    });
});
